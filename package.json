{
  "name": "agent-space",
  "version": "0.1.0",
  "private": true,
  "description": "Human-agent coordination built on presto-ts — derived from essential constraints",
  "scripts": {
    "start": "bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "better-auth": "^1.6.0"
  }
}
