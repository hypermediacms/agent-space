// C6 — Attestation storage
// Uses the adapter's default content schema (id, slug, title, body, status, created_at).
// Attestation fields stored as JSON in `body`. Title carries the version label.
// This respects the adapter's constraint — no custom DDL needed.
